---
phase: 12-vcpkg-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ports/qtpilot/portfile.cmake
  - ports/qtpilot/vcpkg.json
  - ports/qtpilot/usage
autonomous: true

must_haves:
  truths:
    - "vcpkg install qtpilot --overlay-ports=./ports builds probe from source against user's Qt"
    - "Source port auto-detects Qt major.minor version and produces correctly named artifact"
    - "After install, find_package(qtPilot) and target_link_libraries(app qtPilot::Probe) works"
    - "Port works with both Qt5 and Qt6 installations"
    - "Usage instructions are shown after install"
  artifacts:
    - path: "ports/qtpilot/vcpkg.json"
      provides: "Port manifest with no qtbase dependency"
      contains: "qtpilot"
      min_lines: 5
    - path: "ports/qtpilot/portfile.cmake"
      provides: "Source build portfile using vcpkg_from_github"
      contains: "vcpkg_from_github"
      min_lines: 20
    - path: "ports/qtpilot/usage"
      provides: "Post-install usage instructions"
      contains: "find_package(qtPilot)"
  key_links:
    - from: "ports/qtpilot/portfile.cmake"
      to: "CMakeLists.txt"
      via: "vcpkg_cmake_configure invokes project CMakeLists.txt"
      pattern: "vcpkg_cmake_configure"
    - from: "ports/qtpilot/portfile.cmake"
      to: "cmake/qtPilotConfig.cmake.in"
      via: "vcpkg_cmake_install triggers configure_package_config_file"
      pattern: "vcpkg_cmake_install"
---

<objective>
Create the source overlay port for qtPilot that builds the probe from source against the user's existing Qt installation.

Purpose: Enables users to install qtPilot via `vcpkg install qtpilot --overlay-ports=./ports`, compiling the probe against their own Qt (5.15+ or 6.5+) for exact ABI compatibility.

Output: Complete source overlay port at `ports/qtpilot/` with portfile, manifest, and usage instructions.
</objective>

<execution_context>
@C:\Users\stjohnson\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\stjohnson\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-vcpkg-port/12-CONTEXT.md

Key files that define the build/install:
@CMakeLists.txt
@cmake/qtPilotConfig.cmake.in
@cmake/qtPilot_inject_probe.cmake
@src/probe/CMakeLists.txt
@vcpkg.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source overlay port</name>
  <files>
    ports/qtpilot/vcpkg.json
    ports/qtpilot/portfile.cmake
    ports/qtpilot/usage
  </files>
  <action>
Create directory `ports/qtpilot/` if it does not exist.

**ports/qtpilot/vcpkg.json:**
```json
{
  "name": "qtpilot",
  "version": "0.1.0",
  "description": "Qt application introspection and automation library with MCP integration",
  "homepage": "https://github.com/ssss2art/qtPilot",
  "license": "MIT",
  "supports": "windows | linux",
  "dependencies": [
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ]
}
```

IMPORTANT: Do NOT declare qtbase, qtwebsockets, or any Qt dependency. The port relies on Qt already being installed on the system (either via vcpkg or externally). The project's CMakeLists.txt already does find_package(Qt6/Qt5) and will find whatever Qt is available.

**ports/qtpilot/portfile.cmake:**
```cmake
vcpkg_from_github(
    OUT_SOURCE_PATH SOURCE_PATH
    REPO ssss2art/qtPilot
    REF "v${VERSION}"
    SHA512 0  # Placeholder - must be updated when first release tag is created
    HEAD_REF main
)

vcpkg_cmake_configure(
    SOURCE_PATH "${SOURCE_PATH}"
    OPTIONS
        -DQTPILOT_BUILD_TESTS=OFF
        -DQTPILOT_BUILD_TEST_APP=OFF
        -DQTPILOT_ENABLE_CLANG_TIDY=OFF
        -DQTPILOT_DEPLOY_QT=OFF
)

vcpkg_cmake_install()

# Fix up the CMake config files (moves from share/cmake/qtPilot/ to share/qtpilot/)
vcpkg_cmake_config_fixup(PACKAGE_NAME qtPilot CONFIG_PATH share/cmake/qtPilot)

# Remove empty include directory if no headers were installed
# (probe is a shared library loaded via injection, headers aren't typically needed by consumers)
file(REMOVE_RECURSE "${CURRENT_PACKAGES_DIR}/debug/include")

# Remove duplicate tools/binaries from debug
file(REMOVE_RECURSE "${CURRENT_PACKAGES_DIR}/debug/bin/qtPilot-launcher${VCPKG_TARGET_EXECUTABLE_SUFFIX}")
if(EXISTS "${CURRENT_PACKAGES_DIR}/debug/share")
    file(REMOVE_RECURSE "${CURRENT_PACKAGES_DIR}/debug/share")
endif()

# Install usage instructions
file(INSTALL "${CMAKE_CURRENT_LIST_DIR}/usage" DESTINATION "${CURRENT_PACKAGES_DIR}/share/${PORT}")

# Install license
vcpkg_install_copyright(FILE_LIST "${SOURCE_PATH}/LICENSE")
```

Key design notes:
- `SHA512 0` is a placeholder. vcpkg will error on install until a real hash is provided. This is standard for ports under active development — the hash gets updated when the first release tag exists. Include a comment explaining this.
- `-DQTPILOT_BUILD_TESTS=OFF` and `-DQTPILOT_BUILD_TEST_APP=OFF` skip test targets.
- `-DQTPILOT_DEPLOY_QT=OFF` prevents windeployqt from running (vcpkg handles runtime dependencies).
- `vcpkg_cmake_config_fixup` moves the CMake config from the project's install path (`share/cmake/qtPilot`) to vcpkg's expected path (`share/qtpilot`). This needs PACKAGE_NAME qtPilot because our project uses `qtPilot` as the package name.
- The port does NOT strip the debug libraries — vcpkg handles debug/release splitting automatically via `vcpkg_cmake_install()`.

**ports/qtpilot/usage:**
```
qtpilot provides CMake targets:

  find_package(qtPilot CONFIG REQUIRED)
  target_link_libraries(main PRIVATE qtPilot::Probe)

The probe is loaded via injection (LD_PRELOAD on Linux, DLL injection on Windows).
See the project README for injection instructions.
```

IMPORTANT: After creating the files, verify the directory structure is:
```
ports/
  qtpilot/
    portfile.cmake
    vcpkg.json
    usage
```
  </action>
  <verify>
1. `ports/qtpilot/vcpkg.json` exists and is valid JSON (`python -c "import json; json.load(open('ports/qtpilot/vcpkg.json'))"`)
2. `ports/qtpilot/portfile.cmake` contains `vcpkg_from_github`, `vcpkg_cmake_configure`, `vcpkg_cmake_install`, and `vcpkg_cmake_config_fixup`
3. `ports/qtpilot/vcpkg.json` does NOT contain `qtbase` or `qtwebsockets` in dependencies
4. `ports/qtpilot/usage` exists and mentions `find_package(qtPilot)`
5. portfile.cmake passes `-DQTPILOT_BUILD_TESTS=OFF` and `-DQTPILOT_BUILD_TEST_APP=OFF`
6. `vcpkg_cmake_config_fixup` uses `PACKAGE_NAME qtPilot` and `CONFIG_PATH share/cmake/qtPilot` (grep: `vcpkg_cmake_config_fixup(PACKAGE_NAME qtPilot CONFIG_PATH share/cmake/qtPilot)`)
  </verify>
  <done>Source overlay port exists at ports/qtpilot/ with portfile (vcpkg_from_github + cmake build), manifest (no Qt deps), and usage instructions. Port delegates Qt detection entirely to the project's own CMakeLists.txt.</done>
</task>

</tasks>

<verification>
1. `ports/qtpilot/vcpkg.json` is valid JSON with name "qtpilot"
2. `ports/qtpilot/portfile.cmake` uses vcpkg_from_github, vcpkg_cmake_configure, vcpkg_cmake_install, vcpkg_cmake_config_fixup
3. No Qt dependencies declared in vcpkg.json
4. Usage file contains find_package instructions
5. Build options disable tests and test app
</verification>

<success_criteria>
- Source overlay port directory exists at ports/qtpilot/ with all three required files
- Port manifest declares no Qt dependencies (user provides their own Qt)
- Portfile builds using project's existing CMake system with tests disabled
- CMake config fixup relocates qtPilotConfig.cmake to vcpkg-standard path
- Usage instructions explain find_package and injection workflow
</success_criteria>

<output>
After completion, create `.planning/phases/12-vcpkg-port/12-01-SUMMARY.md`
</output>
