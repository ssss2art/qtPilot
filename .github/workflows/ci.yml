name: CI

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "tests/**"
      - "test_app/**"
      - "CMakeLists.txt"
      - "cmake/**"
      - "CMakePresets.json"
      - ".github/workflows/**"
  pull_request:
    branches: [main]
  workflow_dispatch:
  workflow_call:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: jidicula/clang-format-action@654a770daa28443dd111d133e4083e21c1075674 # v4.18.0
        with:
          clang-format-version: "18"
          check-path: "src"

  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { qt: "5.15.2", os: ubuntu-24.04, preset: release,         platform: linux,   compiler: gcc13,  modules: "" }
          - { qt: "5.15.2", os: windows-2022, preset: windows-release,     platform: windows, compiler: msvc17, modules: "" }
          - { qt: "5.15.2", os: windows-2022, preset: windows-x86-release, platform: windows, compiler: msvc17, modules: "", arch: "win32_msvc2019" }
          - { qt: "6.5.3",  os: ubuntu-24.04, preset: release,         platform: linux,   compiler: gcc13,  modules: "qtwebsockets" }
          - { qt: "6.5.3",  os: windows-2022, preset: windows-release, platform: windows, compiler: msvc17, modules: "qtwebsockets" }
          - { qt: "6.8.0",  os: ubuntu-24.04, preset: release,         platform: linux,   compiler: gcc13,  modules: "qtwebsockets" }
          - { qt: "6.8.0",  os: windows-2022, preset: windows-release, platform: windows, compiler: msvc17, modules: "qtwebsockets" }
          - { qt: "6.9.0",  os: ubuntu-24.04, preset: release,         platform: linux,   compiler: gcc13,  modules: "qtwebsockets" }
          - { qt: "6.9.0",  os: windows-2022, preset: windows-release, platform: windows, compiler: msvc17, modules: "qtwebsockets" }
          - { qt: "6.10.0", os: ubuntu-24.04, preset: release,         platform: linux,   compiler: gcc13,  modules: "qtwebsockets" }
          - { qt: "6.10.0", os: windows-2022, preset: windows-release, platform: windows, compiler: msvc17, modules: "qtwebsockets" }
          - { qt: "6.10.0", os: macos-14,       preset: macos-release,   platform: macos,   compiler: clang,  modules: "qtwebsockets", arch_label: arm64 }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Qt
        uses: jurplel/install-qt-action@d325aaf2a8baeeda41ad0b5d39f84a6af9bcf005 # v4.3.0
        with:
          version: ${{ matrix.qt }}
          modules: ${{ matrix.modules }}
          arch: ${{ matrix.arch || '' }}
          cache: true

      - name: Install X11 dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxext-dev libxtst-dev libx11-dev

      - name: Configure
        shell: bash
        run: |
          # install-qt-action sets QT_ROOT_DIR on Linux and Qt5_DIR/Qt6_DIR variously.
          # Qt5_DIR and Qt6_DIR point to lib/cmake/Qt{5,6}/ — walk up to the prefix.
          if [ -n "$QT_ROOT_DIR" ]; then
            QT_PATH="$QT_ROOT_DIR"
          elif [ -n "$Qt6_DIR" ]; then
            QT_PATH="$(cd "$Qt6_DIR/../../.." && pwd)"
          elif [ -n "$Qt5_DIR" ]; then
            QT_PATH="$(cd "$Qt5_DIR/../../.." && pwd)"
          else
            echo "::error::No Qt path found in environment"
            exit 1
          fi
          echo "Using Qt prefix: $QT_PATH"
          cmake --preset ${{ matrix.preset }} -DCMAKE_PREFIX_PATH="$QT_PATH"

      - name: Build
        run: cmake --build --preset ${{ matrix.preset }}

      - name: Test
        run: ctest --preset ${{ matrix.preset }} -LE admin

      - name: Install
        shell: bash
        run: cmake --install build/${{ matrix.preset }} --prefix install/${{ matrix.preset }}

      - name: Bundle test app (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          PRESET="${{ matrix.preset }}"
          # Resolve Qt prefix (same logic as Configure step)
          if [ -n "$QT_ROOT_DIR" ]; then
            QT_PATH="$QT_ROOT_DIR"
          elif [ -n "$Qt6_DIR" ]; then
            QT_PATH="$(cd "$Qt6_DIR/../../.." && pwd)"
          elif [ -n "$Qt5_DIR" ]; then
            QT_PATH="$(cd "$Qt5_DIR/../../.." && pwd)"
          fi
          mkdir -p "install/$PRESET/testapp"
          cp "build/$PRESET/bin/Release/qtPilot-test-app.exe" "install/$PRESET/testapp/"
          # Copy launcher and probe into testapp so everything is self-contained
          cp "install/$PRESET/bin/qtPilot-launcher.exe" "install/$PRESET/testapp/" 2>/dev/null || true
          for f in "install/$PRESET/lib/"qtPilot-probe-*.dll; do
            [ -f "$f" ] && cp "$f" "install/$PRESET/testapp/qtPilot-probe.dll"
          done
          # Run windeployqt on both test app and probe to capture all Qt deps (incl. WebSockets)
          "$QT_PATH/bin/windeployqt.exe" --no-translations "install/$PRESET/testapp/qtPilot-test-app.exe"
          "$QT_PATH/bin/windeployqt.exe" --no-translations "install/$PRESET/testapp/qtPilot-probe.dll" 2>/dev/null || true
          echo "Test app bundle size:"
          du -sh "install/$PRESET/testapp/"

      - name: Bundle test app (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          PRESET="${{ matrix.preset }}"
          QT_MAJOR=$(echo "${{ matrix.qt }}" | cut -c1)
          if [ -n "$QT_ROOT_DIR" ]; then
            QT_PATH="$QT_ROOT_DIR"
          elif [ -n "$Qt6_DIR" ]; then
            QT_PATH="$(cd "$Qt6_DIR/../../.." && pwd)"
          elif [ -n "$Qt5_DIR" ]; then
            QT_PATH="$(cd "$Qt5_DIR/../../.." && pwd)"
          fi
          mkdir -p "install/$PRESET/testapp/lib" "install/$PRESET/testapp/plugins/platforms"
          cp "build/$PRESET/bin/qtPilot-test-app" "install/$PRESET/testapp/"
          # Copy launcher and probe into testapp so everything is self-contained
          cp "install/$PRESET/bin/qtPilot-launcher" "install/$PRESET/testapp/" 2>/dev/null || true
          for f in "install/$PRESET/lib/"libqtPilot-probe-*.so; do
            [ -f "$f" ] && cp "$f" "install/$PRESET/testapp/qtPilot-probe.so"
          done
          # Copy minimal Qt runtime (including WebSockets for the probe)
          for lib in libQt${QT_MAJOR}Core.so* libQt${QT_MAJOR}Gui.so* libQt${QT_MAJOR}Widgets.so* libQt${QT_MAJOR}Network.so* libQt${QT_MAJOR}WebSockets.so* libQt${QT_MAJOR}DBus.so* libQt${QT_MAJOR}XcbQpa.so* libicui18n.so* libicuuc.so* libicudata.so*; do
            find "$QT_PATH/lib" -name "$lib" -exec cp {} "install/$PRESET/testapp/lib/" \; 2>/dev/null || true
          done
          cp "$QT_PATH/plugins/platforms/libqxcb.so" "install/$PRESET/testapp/plugins/platforms/" 2>/dev/null || true
          cp "$QT_PATH/plugins/platforms/libqminimal.so" "install/$PRESET/testapp/plugins/platforms/" 2>/dev/null || true
          # Create wrapper script
          cat > "install/$PRESET/testapp/qtPilot-test-app.sh" << 'WRAPPER'
          #!/bin/sh
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export LD_LIBRARY_PATH="$DIR/lib:$LD_LIBRARY_PATH"
          export QT_PLUGIN_PATH="$DIR/plugins"
          exec "$DIR/qtPilot-test-app" "$@"
          WRAPPER
          chmod +x "install/$PRESET/testapp/qtPilot-test-app.sh"
          echo "Test app bundle size:"
          du -sh "install/$PRESET/testapp/"

      - name: Bundle test app (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          PRESET="${{ matrix.preset }}"
          QT_MAJOR=$(echo "${{ matrix.qt }}" | cut -c1)
          if [ -n "$QT_ROOT_DIR" ]; then
            QT_PATH="$QT_ROOT_DIR"
          elif [ -n "$Qt6_DIR" ]; then
            QT_PATH="$(cd "$Qt6_DIR/../../.." && pwd)"
          elif [ -n "$Qt5_DIR" ]; then
            QT_PATH="$(cd "$Qt5_DIR/../../.." && pwd)"
          fi
          mkdir -p "install/$PRESET/testapp"
          # Copy binary (flat, not .app bundle — CMake doesn't set MACOSX_BUNDLE for test app)
          cp "build/$PRESET/bin/qtPilot-test-app" "install/$PRESET/testapp/"
          cp "install/$PRESET/bin/qtPilot-launcher" "install/$PRESET/testapp/" 2>/dev/null || true
          for f in "install/$PRESET/lib/"libqtPilot-probe-*.dylib; do
            [ -f "$f" ] && cp "$f" "install/$PRESET/testapp/qtPilot-probe.dylib"
          done
          # Wrapper script (sets DYLD paths for Qt frameworks)
          cat > "install/$PRESET/testapp/qtPilot-test-app.sh" << 'WRAPPER'
          #!/bin/sh
          DIR="$(cd "$(dirname "$0")" && pwd)"
          export DYLD_FRAMEWORK_PATH="$DIR:${DYLD_FRAMEWORK_PATH:-}"
          export QT_PLUGIN_PATH="$DIR/plugins"
          exec "$DIR/qtPilot-test-app" "$@"
          WRAPPER
          chmod +x "install/$PRESET/testapp/qtPilot-test-app.sh"
          echo "Test app bundle size:"
          du -sh "install/$PRESET/testapp/"

      - name: Ad-hoc sign binaries (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          PRESET="${{ matrix.preset }}"
          codesign --sign - --force "install/$PRESET/bin/qtPilot-launcher" 2>/dev/null || true
          for f in "install/$PRESET/lib/"libqtPilot-probe-*.dylib; do
            [ -f "$f" ] && codesign --sign - --force "$f" || true
          done

      - name: Upload artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: qtpilot-${{ matrix.preset }}-qt${{ matrix.qt }}-${{ matrix.compiler }}${{ matrix.arch_label && format('-{0}', matrix.arch_label) || '' }}
          path: install/${{ matrix.preset }}

  python:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          # --- Python version coverage -------------------------------------
          # One leg per interpreter in `requires-python` / the classifiers, on
          # the default resolve. Previously CI tested only 3.11 while the
          # package advertised 3.11-3.14.
          - name: py3.11
            python: "3.11"
            extras: "dev"
            pip_flags: ""
            expect_revision: ""
          - name: py3.12
            python: "3.12"
            extras: "dev"
            pip_flags: ""
            expect_revision: ""
          - name: py3.13
            python: "3.13"
            extras: "dev"
            pip_flags: ""
            expect_revision: ""
          - name: py3.14
            python: "3.14"
            extras: "dev"
            pip_flags: ""
            expect_revision: ""
          # --- MCP revision coverage ---------------------------------------
          # Pinned to the floor interpreter: the SDK legs are about protocol
          # revision, and the version axis is already covered above.
          - name: mcp-stable
            python: "3.11"
            extras: "dev,mcp-stable"
            pip_flags: ""
            expect_revision: "2025-11-25"
          # MCP 2026-07-28 (FastMCP 4). Pre-release until FastMCP 4 GAs, so
          # these legs are allowed to fail without failing the workflow.
          # Run on both the oldest and newest interpreter -- this is the path
          # being proven out, so it gets the widest check.
          - name: mcp-next
            python: "3.11"
            extras: "dev,mcp-next"
            pip_flags: "--pre"
            expect_revision: "2026-07-28"
          - name: mcp-next-py3.14
            python: "3.14"
            extras: "dev,mcp-next"
            pip_flags: "--pre"
            expect_revision: "2026-07-28"
    name: python (${{ matrix.name }})
    # The 2026-07-28 path tracks an upstream beta; a broken pre-release upstream
    # must not redden the whole workflow. Drop this when FastMCP 4 ships stable.
    continue-on-error: ${{ startsWith(matrix.name, 'mcp-next') }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ matrix.python }}

      - name: Install
        working-directory: python
        run: pip install ${{ matrix.pip_flags }} -e ".[${{ matrix.extras }}]"

      - name: Report resolved MCP stack
        working-directory: python
        run: python -c "import qtpilot._mcp_compat as c; print(c.describe())"

      - name: Assert MCP protocol revision
        if: matrix.expect_revision != ''
        working-directory: python
        env:
          EXPECTED: ${{ matrix.expect_revision }}
        run: |
          python - <<'PY'
          import os, sys
          import qtpilot._mcp_compat as c
          expected = os.environ["EXPECTED"]
          actual = c.protocol_revision()
          if actual != expected:
              sys.exit(
                  f"Expected MCP revision {expected}, got {actual} "
                  f"(fastmcp {c.fastmcp_version()})"
              )
          print(f"OK: {actual} via fastmcp {c.fastmcp_version()}")
          PY

      - name: Test
        working-directory: python
        run: pytest -v
